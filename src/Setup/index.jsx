import React from 'react'

export default () => {
  return <div className='setup'>
    <p><a href=''>Μïmis</a> runs from your local machine. To use it:</p>
    <p>You can load an IPFS file structure:</p>
    <p>Or, you can copy a processed cache from the cloud:</p>
  </div>
}