<%= form_with(model: filename, local: true) do |form| %>
  <% if filename.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filename.errors.count, "error") %> prohibited this filename from being saved:</h2>

      <ul>
        <% filename.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
